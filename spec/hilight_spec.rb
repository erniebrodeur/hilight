RSpec.describe Hilight do
  it "has a version number" do
    expect(Hilight::VERSION).not_to be nil
  end

  it "is expected to highlight the output of 'rspec'"
  it "is expected to execute everything on ARGV"
  it "is expected to highlight the output"
  it "is expected to lookup highlight template"
end
